import { AppScreen } from '@/foundation/components/layout/AppScreen';
import { AppText } from '@/foundation/components/layout/AppText';

export function TrackersScreen() {
  return (
    <AppScreen>
      <AppText variant="pageTitle">Trackers/Setup</AppText>
      <AppText>Category, Section, and Option management starts here.</AppText>
    </AppScreen>
  );
}
